# 02. BigQuery テーブル設計と SQL 整備

## 目的
SEO モニタリングに必要な BigQuery テーブルと SQL 管理方針を固める。

## 重要事項
- 【】 作業を始める前に必ず本チケットを確認し、今回の作業箇所をチェックしてから着手する
- 【】 チケットは原則として番号順に進める。順番を変更する場合は理由を作業ログに残す
- 【】 このチケットで行った作業は都度 `作業ログ` に追記する

## 今回の作業箇所
- 【×】 `daily_rankings` テーブル定義を作成する
- 【×】 `rewrites` テーブル定義を作成する
- 【×】 `internal_links` テーブル定義を作成する
- 【×】 `tracked_keywords` テーブル定義を作成する
- 【×】 日付パーティション方針と `url`, `keyword` のクラスタリング方針を明記する
- 【×】 SQL を `sql/` 配下に分離する運用ルールを決める
- 【×】 パーティション条件必須のクエリテンプレートを用意する
- 【×】 schema 変更時に migration を追加する運用を定義する

## TODO
- 【×】 `daily_rankings` テーブル定義を作成する
- 【×】 `rewrites` テーブル定義を作成する
- 【×】 `internal_links` テーブル定義を作成する
- 【×】 `tracked_keywords` テーブル定義を作成する
- 【×】 日付パーティション方針と `url`, `keyword` のクラスタリング方針を明記する
- 【×】 SQL を `sql/` 配下に分離する運用ルールを決める
- 【×】 パーティション条件必須のクエリテンプレートを用意する
- 【×】 schema 変更時に migration を追加する運用を定義する

## 完了条件
- 必要テーブルの作成手順が明文化されている
- 初期 SQL を流せば Phase 1 の保存先を用意できる

## 依存
- [01. プロジェクト基盤セットアップ](./01-project-bootstrap.md)

## 作業ログ
- 2026-03-16: チケット作成。
- 2026-03-16: 重要事項と作業ログ運用を追加。
- 2026-03-16: チケット内容を確認し、今回の作業箇所をチェックしたうえで `02` に着手。
- 2026-03-16: `sql/migrations/` に 4 本の BigQuery DDL を追加し、`daily_rankings`, `rewrites`, `internal_links`, `tracked_keywords` の作成定義を整理。
- 2026-03-16: 各テーブルのパーティション方針とクラスタリング方針を DDL と `sql/README.md` に明記。
- 2026-03-16: `sql/queries/` にパーティション条件付きの KPI 集計テンプレートを追加。
- 2026-03-16: `sql/README.md` を追加し、migration 追加ルールと SQL 配置運用を定義。
